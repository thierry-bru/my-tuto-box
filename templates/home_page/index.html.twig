{% extends 'base.html.twig' %}

{% block title %}Hello HomePageController!{% endblock %}

{% block body %}

<header>
    <nav class="navbar navbar-expand-lg">
  <div class="container-fluid d-flex align-items-end">
        <a class="navbar-brand" href="{{ path('app_home_page') }}"><img src="{{ asset('pictures/interface/logo-tuto-box.png')}}"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
    {{ include ('home_page/menu.html.twig') }}
    </div>
    </div>
      <turbo-frame id="profile"  src="{{ path('app_home_page_profile') }}">
            </turbo-frame>
    </nav>
</header>
    
    <main>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasMenuProfile" aria-labelledby="offcanvasMenuProfileLabel">
  <div class="offcanvas-header">
  <img class="rounded-circle shadow-sm bg-body-tertiary avatar m-3"src="{{ asset('pictures/avatars/myPilllz25.png')}}">
    <h5 class="offcanvas-title" id="offcanvasMenuProfileLabel">Profile</h5>
    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
  </div>
  <div class="offcanvas-body d-flex flex-column mb-3">
  <p>Jehan le Preux</p>
  <p>Pourfendeur de Tutoriel</p>
  <div class="progress" role="progressbar" aria-label="Basic example" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100">
  <div class="progress-bar" style="width: 25%"></div>
</div>
<a href="#">Notifications</a>
<a href="#">Profil</a>
<a href="#">Progrés</a>
<a href="#">Données personnelles</a>
  </div>
</div>
{% endif %}
        <turbo-frame id="content" src="{{ path('app_home_page_welcome') }}"></turbo-frame>
    </main>
    <footer class="bg-dark fixed-bottom">
      footer
    </footer>



{% endblock %}
